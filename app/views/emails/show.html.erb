<div class="col-sm-8">
  <div class="card">
    <div class="card-header">
      Featured
    </div>
    <div class="card-body">
      <h5 class="card-title"><%= @email.object %> </h5>
      <p class="card-text"><%= @email.body %> </p>
      <%= link_to 'Supprimer', email_path(email.id), remote: true, data: {:confirm => 'Are you sure?'}, :method => :delete, :class => "btn btn-primary"  %>
      <a href="#" class="btn btn-outline-primary">Marquer comme lu</a>
    </div>
  </div>
</div>
